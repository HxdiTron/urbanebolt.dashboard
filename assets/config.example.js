/**
 * Runtime configuration for Supabase connection.
 *
 * LOCAL DEVELOPMENT:
 *   Copy this file to `assets/config.js` and fill in your values.
 *   config.js is gitignored — your keys stay out of the repo.
 *
 * VERCEL DEPLOYMENT:
 *   Set these environment variables in Vercel dashboard:
 *     - SUPABASE_URL
 *     - SUPABASE_ANON_KEY
 *   The build script (npm run build) auto-generates config.js from env vars.
 */
window.SUPABASE_URL = "https://YOUR_PROJECT.supabase.co";
window.SUPABASE_ANON_KEY = "YOUR_SUPABASE_ANON_KEY";

